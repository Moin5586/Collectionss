static void Main(string[] args)
{

    List<int> numbers = new List<int> { 1, 2, 3, 4, 5, 6, 7, 8 };
    IEnumerable<int> evenNum = numbers.Where(n => n % 2 == 0);
    foreach (int var in evenNum)
    {
        Console.WriteLine(var);
    }
    Console.ReadLine();

}
